apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: efs-pvc
  namespace: efs-demo

spec:
  accessModes:
    - ReadWriteMany

  storageClassName: efs

  resources:
    requests:
      storage: 5Gi